import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'manual-biometric-prompt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
